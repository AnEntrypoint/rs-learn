## Resolved mutable: mut-1783075881596

codesearch for Mutex/RwLock/lock() across crate returns only crates/wasm/src/wasm_export.rs:7 static SESSION and :40 SESSION.lock() -- single lock site, no nested acquisition, no ordering hazard possible