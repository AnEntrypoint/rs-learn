crates/wasm/src/wasm_export.rs:18:21 rs_learn_free
pub extern "C" fn rs_learn_free(ptr: *mut u8, len: usize) {
    if ptr.is_null() || len == 0 { return; }
    unsafe { let _ = Vec::from_raw_parts(ptr, len, len); }
}