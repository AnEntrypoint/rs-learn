crates/wasm/src/wasm_export.rs:27:34 leak_bytes
fn leak_bytes(bytes: Vec<u8>) -> u64 {
    if bytes.is_empty() { return 0; }
    let len = bytes.len() as u32;
    let mut v = bytes;
    let ptr = v.as_mut_ptr();
    core::mem::forget(v);
    pack(ptr, len)
}