crates/wasm/src/wasm_host.rs:29:36 take_bytes
unsafe fn take_bytes(packed: u64) -> Vec<u8> {
    let (ptr, len) = unpack(packed);
    if ptr == 0 || len == 0 {
        return Vec::new();
    }
    let slice = core::slice::from_raw_parts(ptr as *const u8, len as usize);
    slice.to_vec()
}