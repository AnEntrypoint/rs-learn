crates/wasm/src/router/core.rs:9:17 Route
pub struct Route {
    pub model: String,
    pub context_bucket: u8,
    pub temperature: f32,
    pub top_p: f32,
    pub confidence: f32,
    pub algo: &'static str,
    pub exploration: bool,
}