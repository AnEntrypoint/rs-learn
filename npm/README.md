# rs-learn (npm wrapper)

Downloads and runs the platform-native `rs-learn` binary built on GitHub Releases.

```
bun x rs-learn --help
# or
npx rs-learn --help
```

Supported platforms: linux x64/arm64, darwin x64/arm64, windows x64/arm64.

Source and docs: https://github.com/AnEntrypoint/rs-learn
